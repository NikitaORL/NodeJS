<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title><%= news.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body class="container mt-4">
<a href="/" class="btn btn-secondary mb-3">Avaleht</a>
<h1 class="mb-3"><%= news.title %></h1>
<p class="mb-4"><%= news.content %></p>
<div class="d-flex gap-2 mb-5">
    <a href="/edit/<%= news.id %>" class="btn btn-primary">
        <i class="bi bi-pencil-square"></i> Uuenda
    </a>
    <form action="/delete/<%= news.id %>" method="POST" class="d-inline" onsubmit="return confirm('Удалить новость?');">
        <button class="btn btn-danger">
            <i class="bi bi-trash"></i> Kustuta
        </button>
    </form>
    <a href="/" class="btn btn-outline-secondary">Tagasi</a>
</div>
</body>
</html>
