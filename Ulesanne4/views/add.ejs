<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Lisa uudis</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
<a href="/" class="btn btn-secondary mb-3">Avaleht</a>
<h1>Lisa uudis</h1>

<% if(errors.length>0){ %>
    <div class="alert alert-danger">
        <% errors.forEach(e=>{ %>
            <p><%= e.msg %></p>
        <% }) %>
    </div>
<% } %>

<form action="/add" method="POST">
    <div class="mb-3">
        <input type="text" name="title" class="form-control" placeholder="Pealkiri" value="<%= values.title %>">
    </div>
    <div class="mb-3">
        <textarea name="content" class="form-control" placeholder="Sisukord"><%= values.content %></textarea>
    </div>
    <button class="btn btn-primary">Salvesta</button>
</form>
</body>
</html>
